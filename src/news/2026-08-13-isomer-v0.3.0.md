---
title: "isomer v0.3.0: judgment trained on 30 years of attacks — plus cleave & Scan v2.7.0"
date: 2026-08-13
summary: "This cycle was about shaping isomer into something useful: verdicts calibrated against 30 years of real supply-chain attacks, cleanups no longer read as attacks, and the LLM can never raise the CI gate. cleave and Scan v2.7.0 carry the changes isomer needed. First release where all three ship prebuilt binaries."
---

**[isomer](/isomer/) v0.3.0 judges the shape of a change, not the size of
it.** Its heuristics are calibrated against the
[compendium](https://isotope13.ai/compendium/) — 30 years of real
supply-chain attacks, from Ken Thompson's compiler to last month's npm
worms. A focused source implant, a cross-domain capability cluster gained on
a tiny version bump, an encrypted payload in a same-version repack: each
escalates on its own. The fixture set now includes unrealircd's 2010
backdoored tarball, and it fails CI.

**Cleanups are no longer attacks.** Removing a backdoor used to look like a
large, suspicious change. Remediation is now a first-class outcome with its
own severity floor, and the output gains a "removed" section. Only the risk
*jump* counts — a scary package's ordinary update isn't condemned for its
baseline. The LLM can raise the displayed severity but never the CI gate.

**Its dependencies grew what it needed.** [cleave](/cleave/) v2.7.0 analyzes
identical bytes once across both sides of a diff, skips unchanged members,
and keeps files that are hostile on *both* sides visible instead of
filtering them as "no change". [Scan](/scan/) v2.7.0 grades every embedded
file — the old 100-member cap meant a hostile file at index 347 never
elevated its parent — and applies registry findings to fetched dependencies
before grading. Both take `--zip-password` for encrypted archives.

**Prebuilt binaries, all three, for the first time.** macOS builds are
signed and notarized; cleave and Scan also cover Linux, Windows, and the
BSD/illumos family. Installers are coming soon.

Release notes:
[isomer v0.3.0](https://github.com/atomdrift-project/isomer/releases/tag/v0.3.0) ·
[cleave v2.7.0](https://github.com/atomdrift-project/cleave/releases/tag/v2.7.0) ·
[scan v2.7.0](https://github.com/atomdrift-project/scan/releases/tag/v2.7.0)

```
brew upgrade atomdrift-project/tap/cleave atomdrift-project/tap/scan
```
